import styled from "styled-components";

export const AccommodationSearchInputBox = styled.input`
  width: 100%;
  height: auto;
  border: 1px solid #d9d9d9;
  border-radius: ${({ theme }) => theme.box.radius};
`;
