import { styled } from "styled-components";

const CalendarLayout = styled.div`
  width: 100%;
  height: auto;
  // position: fixed;
`;

const AccommodationCalender = () => {
  return <CalendarLayout>123</CalendarLayout>;
};

export default AccommodationCalender;
